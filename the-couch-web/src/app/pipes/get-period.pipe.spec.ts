import { GetPeriodPipe } from './get-period.pipe';

describe('GetPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
